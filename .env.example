# Google Cloud project with "Places API (New)" enabled + billing attached.
# Used by /api/photos and /api/reviews. Server-side only — never exposed to the client.
# Copy this file to .env.local and fill it in. .env.local is gitignored.
GOOGLE_PLACES_API_KEY=

# Open-Meteo (weather + geocoding) needs no key.
