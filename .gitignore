# env — NEVER commit
.env
.env.local
.env*.local

# deps
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/
next-env.d.ts

# vercel
.vercel

# generated
# data/coords.json is NOT ignored: lib/data.ts imports it, so the build needs it.
# It holds only public town coordinates resolved from Open-Meteo — no secrets.
# Regenerate with: bun run geocode
*.tsbuildinfo

# os
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/
